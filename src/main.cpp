#include <iostream>

using namespace std;

// Hello, this is a c++ project template
// Use ./gen_compile_commands.sh to generate compile_commands (you need direnv), this will be make lsp work better
// nix build will build project to result symlink
// nix run will build and run project

int main() {
	cout << "It's just template, dude!\n";
}
